import React from 'react';
import { storiesOf } from '@storybook/react';
import {{pascalCase name}} from 'Elements/{{pascalCase name}}';
import notes from './{{pascalCase name}}.notes.md';

storiesOf('{{pascalCase type}}', module).add(
    '{{pascalCase name}}',
    () => (
        <{{pascalCase name}} />
    ),
    { notes }
);