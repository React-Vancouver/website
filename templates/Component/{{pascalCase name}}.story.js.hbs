import React from 'react';
import { storiesOf } from '@storybook/react';
import {{pascalCase name}} from '@{{lowerCase type}}s/{{pascalCase name}}';
import notes from './{{pascalCase name}}.notes.md';

storiesOf('{{pascalCase type}}s', module).add(
    '{{pascalCase name}}',
    () => (
        <{{pascalCase name}} />
    ),
    { notes }
);